# Event Report — 2026-07-18

## Phase P.1 — Production Workflow Expansion（持续优化循环启动）

**PR:** #37 (squash merged → main `a7c1029`)
**分支:** feat/phase-p1-workflow-expansion
**风险等级:** 🟢 LOW · Preflight SHA `8e3371c` ✅

### 完成内容

| 步骤 | 交付物 | 验证 |
|:-----|:-------|:-----|
| P.1.1 执行矩阵 | execution-matrix.yaml + veritas/a3/ucampus 三条真实 workflow | veritas **558 passed** · a3 **1074 passed** · project.* 遥测首次落盘 |
| P.1.2 使用数据 | skill_usage_collector.py 三源合并 | 149 技能画像: 活跃 66 (44.3%), 从未使用 83, 闲置 12 |
| P.1.3 优化循环 | optimization_loop.py R1-R4 → 提案 | 11 条 pending (5×P3+6×P4), 幂等四轮复验 0 新增 |
| P.1.4 无人值守 | production-nightly.sh + systemd timer | 真实触发 exit 0, 🟢 NIGHTLY COMPLETE, NEXT 07-19 09:31 |

### 过程中发现并处理的真实缺陷

1. **A3 .venv 缺依赖** — fastapi/numpy/httpx/scikit-learn 缺失致 9 个测试文件无法收集 → 补装后 1074 passed
2. **自研 P3 限流语义 bug** — "每轮新增5条"重复运行会绕过限流持续超发 → 改为"pending 队列总量≤5" + 撤回超发 5 条 + 历史裁决排除
3. **🔴 Kernel `KERNEL_DUAL_STORE` 缺陷** — approval_manager (approvals/*.json) 与 proposal_store (proposals.jsonl) 双存储不联通, reject 返回 error 却不抛异常静默失败 → 记入 error-registry L1, 状态变更统一走 proposal_store.update_status 并检查返回值, kernel 修复列入 P.2

### 复盘（Feedforward + Feedback 配对）

**做得对:**
- 接入前先试跑（A3 收集错误在设计 workflow 前就暴露并修复）
- 限流 bug 靠"重复运行验证幂等"抓出——单轮测试永远发现不了
- reject 静默失败靠"操作后复查队列数量"抓出——教训: **任何治理操作必须检查返回值并复核状态**
- 优化循环安全边界完整: 人批 + 限额 + core 豁免 + 历史裁决排除

**教训:**
- Kernel B.6.5 "60/60 PASS" 没覆盖 approval_manager 与 proposal_store 的集成路径——单元验证≠集成验证, P.2 需补集成用例
- 静默失败是最危险的失败模式; 治理链路上所有 API 返回值必须显式断言

**下一步 (P.2 候选):**
- Kernel 双存储统一修复（proposal_store 为唯一权威）
- 提案审批 UX（monitor 中一键 approve/reject）
- adapter 层 83 个未使用技能的分批治理（P3 每轮 5 条滚动）

---

## Phase P.0 — Production Bootstrap（Hermes Skill OS 进入生产运营）

**PR:** #36 (squash merged → main `d95e349`)
**分支:** feat/phase-p0-production-bootstrap
**风险等级:** 🟡 MEDIUM · Preflight SHA `98959b5` ✅

### 完成内容

| 步骤 | 交付物 | 验证 |
|:-----|:-------|:-----|
| Assessment | `docs/hermes-phase-p0-production-bootstrap-assessment.md` — 4 项发现 (2🔴 2🟡) | Human gate 已呈报 |
| P.0.1 资产固化 | Registry v1.1 入仓 + Kernel 34 文件镜像 + Wave 快照存档 | G1/G2 ✅ |
| P.0.2 每日入口 | `production-daily.sh` + 26 项检查引擎 + AGENTS.md | G3 🟢 ALL GREEN |
| P.0.3 运行配置 | a3/veritas/ucampus runtime.yaml（与 Registry project.* 对齐） | 技能名 12/12 核对 ✅ |
| P.0.4 workflow+监控 | 运行器全管道 + daily-production-report + monitor | G4/G5 ✅ 3条遥测落盘 |

### 关键修复（首轮真实运行抓出）

1. **🔴 Registry v1.1 仅存 /tmp** — Wave 4 宣布 release 但 149 条正本只在
   `/tmp/hermes-wave4-snapshots/`，一次重启即丢失 Phase A 全部成果
   → 提升入仓 + 字节级同步部署版（SHA-256 一致）
2. **🔴 55 份 Phase A/B/C 治理文档悬空** untracked → 全部纳入版本控制
3. **🟡 Registry 字节漂移** 仓库副本 ≠ 部署版格式 → 以部署版为正本
4. **🟡 B.6 验证残留污染生产遥测** t2/q1/v6-skill/gv1/gv2 测试记录
   → 21 文件归档 `~/.hermes/runtime/archive/b6-validation/`（保留证据）

### 真实执行证据

```
workflow run-20260718-180830: 3/3 SUCCESS (53ms)
telemetry: exec-1784369310055/066/083 落盘 2026-07-18.jsonl
health: harness-preflight/skill-manager/task-progress 全部 HEALTHY(96)
governance: 0 pending · quarantine: 0
```

### 复盘（Feedforward + Feedback 配对）

**做得对:**
- 铁律第 0 步机械执行：先 preflight 再动手（本次无违规）
- Assessment 先行发现 /tmp 数据丢失风险 —— 晚一次重启就是灾难
- 每日检查器首轮即抓出 2 个真实问题，证明"真实运行 > 纸面验证"
- 归档而非删除（B.6 残留 21 文件保留证据链）
- PR 自审三重扫描（PII/凭据/registry），命中项逐条人工核验为误报

**教训:**
- **"PRODUCTION RELEASE" ≠ 持久化**。Wave 4 报告写了 release，但正本落在
  /tmp——以后任何 release 声明必须附"正本落盘路径 + git 提交"两项证据
- 治理文档产出后应随 Phase 即时提交，不应积压 55 份到下一 Phase 才入库
- Kernel 部署区 (~/.hermes) 与仓库镜像需每日 diff 校验（已纳入 daily 26 项检查）

**下一步 (P.1 候选):**
- a3/veritas/ucampus 各自的真实业务 workflow 接入（当前仅自托管例程）
- production-daily.sh 接入 cron/systemd timer 实现无人值守
- 遥测保留策略（90天）自动归档执行
