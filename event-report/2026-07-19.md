# Event Report — 2026-07-19

## HPP v0.1 — Hermes Execution Progress Protocol W1–W3（black-box 长任务遥测）

**PR:** 待创建（feat/hpp-progress-protocol，等待 Human Gate merge）
**分支:** feat/hpp-progress-protocol（自 main b3211a3）
**风险等级:** 🟡 MEDIUM（kernel 新模块 + registry schema bump）· Preflight SHA `b3211a3` ✅
**Human Gate:** APPROVED WITH CONDITIONS — W1/W2/W3 批准，W4（executor 钩子）PAUSED

### 完成内容

| Wave | 交付物 | 验证 |
|:--|:--|:--|
| W1 `cb7ca7d` | progress_tracker + progress_watchdog + manifest capability `hermes.core.telemetry.progress` + 20 单测 | 20 passed；真实 store 冒烟（exec-hpp-smoke 完整事件链）；events 流零污染（17 条不变） |
| W2 `2e057c2` | progress_cli + progress-watch.sh（4 探针模板）+ production-monitor ⏳长任务区块 | 实战：watcher 挂载运行中的 A3 release build（exec-a3-release-build），降级心跳路径实测 |
| W3 `5330914` | Registry v1.2（fields += observability，149 条零迁移）+ manifest compat v1.2 + daily 校验重定向 | kernel 25 passed；production_daily 🟢 ALL GREEN (26 checks)；双副本 sha256 一致 |

### 设计要点与偏差记录

- **兼容性修正**（对提案 §3.2）：progress 事件走独立 `progress-events/` 流，
  不混入 `events/`——避免污染 metrics_aggregator 成功率统计。W1 报告已声明。
- 生命周期强校验：started → updated* → terminal，违规抛 ProgressLifecycleError（6 测试覆盖）。
- watchdog 为监督者角色：可直接改写快照（属主进程可能已死），2×interval→WARNING，5×→BLOCKED。
- W4 未动 executor.py（PAUSED，待 W1-W3 验证后另行提案）。

### 复盘

1. **实证驱动设计**：协议直接来自当日 Docker Release 构建的真实黑盒痛点，
   docker_build 探针即当晚人工监控流程（du 采样/连接计数/速率估算）的 1:1 固化。
2. **兼容性红线先行**：动手前先读 collector/event_store/metrics_aggregator 与 daily
   校验的硬编码断言，发现"同流写入会污染指标"与"daily 硬编码 1.1.0"两个暗雷，
   全部在实施内消解——零回归证据是 daily ALL GREEN。
3. **测试用例自身也会错**：speed/ETA 测试首版回填错样本点（dt≈0），单测红灯暴露的
   是测试 bug 而非实现 bug——修测试而非迁就实现。
4. 业务仓库（A3/VC）零接触 ✅；不自动 merge ✅。
