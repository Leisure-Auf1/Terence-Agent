{
  "name": "veritas-test-run",
  "description": "Veritas-Core 结构化测试运行 — 真实业务 workflow：make test 全量 pytest",
  "version": "1.0.0",
  "caller_tier": 2,
  "caller_scope": "project",
  "governance": {
    "constitution": "v1.0",
    "registry": "v1.1",
    "telemetry": "required",
    "namespace": "project.veritas.*"
  },
  "steps": [
    {
      "name": "veritas-structured-tests",
      "skill": "veritas-core",
      "intent": "veritas 结构化测试 runtime 基准 experience memory",
      "action": "cd ~/Veritas-Core && make test 2>&1 | tail -1 | grep -E '[0-9]+ passed'",
      "timeout_s": 300,
      "critical": true
    }
  ]
}
